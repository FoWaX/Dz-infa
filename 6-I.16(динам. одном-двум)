#include <iostream>
using namespace std;

int main()
{
    int n;
    cin >> n;
    int* a = new int[n];
    for (int i = 0; i < n;i++)
    {
        a[i] = i;
    }
    int x, y;
    cin >> x >> y;
    for (int f = 0; f < n; f++)
    {
        if (a[f] < x || a[f] > y)
        {
            cout << f << endl;
        }
    }
    delete[] a;
}



