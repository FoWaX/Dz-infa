#include <iostream>
using namespace std;

int main()
{
    int n = 10;
    int a[10];
    for (int i = 0; i < n;i++)
    {
        a[i] = i;
    }
    int x, y;
    cin >> x >> y;
    for (int f = 0; f < n; f++)
    {
        if (a[f] < x || a[f] > y)
        {
            cout << f << endl;
        }
    }
}


