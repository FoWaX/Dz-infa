#include <iostream>
using namespace std;

int main()
{
    int n = 10;
    int a[10];
    int flag;
    int k = 0;
    for (int i = 0; i < n; i++)
    {
        int q;
        cin >> q;
        a[i] = q;
    }
    for (int i = 0; i < n; i++)
    {
        int f, x, g;
        f = -10;
        x = a[i];
        flag = 0;
        if ((x / 10) % 10 == x % 10)
        {
            f = x % 10;
        }
        while (x > 0)
        {
            g = x % 10;
            if (g != f)
            {
                flag = 1;
            }
            x /= 10;
        }
        if (flag == 1)
        {
            cout << a[i] << endl;
            for (int z = i; z < n - 1; z++)
            {
                a[z] = a[z + 1];
            }
            n--;
            i--;
        }
    }
    for (int i = 0; i < n; i++)
    {
        cout << a[i] << " ";
    }
    изменить!!!!
}
