
#include <iostream>
using namespace std;
#include <cmath>
#include <iomanip>
int main()
{
	int a, b, c;
	double p;
	cin >> a >> b >> c;
	p = double(a + b + c)/ 2;
	cout << fixed << setprecision(4) << sqrt(p * (p - a) * (p - b) * (p - c));
}

