#include <iostream>
#include <cmath>
using namespace std;

double storona(double s)
{
	return sqrt((s * 4) / sqrt(3));
}
double gipoten(double c)
{
	return sqrt(c * c + c * c);
}
int main()
{
	double b,d,n,p;
	cin >> n;
	d = storona(n);
	b = gipoten(d);
	p = 3 * d + b;
	cout << p;
}
