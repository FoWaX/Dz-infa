а) дублирование последнего элемента
list
за O(n), потому для этого надо пройти по всему списку из n элементов
vector
происходит доабвление всего одного элемента в конец, поэтому за O(1)
б)удаление первого элемента
list
O(1), на нужно только назначить указатель на след. элемент, что происходит за O(1)
vector
O(n), ведь для удаления одного элемента нужно все сдвинуть на 1 ячейку
в)Вставить элементы равные нулю до и после первого минимального.
list
поиск минимального элемента происходит за O(n), вставка элемента также за O(n) поэтому вставить элементы равные нулю до и 
после первого минимального таже за O(n)
vector
поиск минимального элемента происходит за O(n), вставка элемента также за  плюс сдвиг всеэ элементов на 2 за O(n) поэтому вставить элементы 
равные нулю до и после первого минимального таже за O(n)
г) Заменить средний элемент на последний чётный. 
list и vector
поиск среднего элемента происходит за O(n), поиск последнего четного также за O(n) поэтому заменить средний элемент на последний чётный за O(n)
