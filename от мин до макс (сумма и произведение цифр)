#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
	int a,x,y;
	cin >> a;
	x =  abs(a) % 10;
	y = abs(a) / 10;
	a = x + y;
	x = x * y;
	for (int i = min(x, a); i <= max(x, a);i++)
	{
		cout << i << endl;
	}
}

